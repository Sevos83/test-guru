<h1>Information about test '<%= @test.title %>'</h1>

<p>
  <ul>
    <li>
      <strong>Название:</strong>
      <%= @test.title %>
    </li>

    <li>
      <strong>Уровень:</strong>
      <%= @test.level %>
    </li>

    <li>
      <strong>Количество вопросов:</strong>
      <%= @test.questions.count %>
    </li>
  </ul>
</p>

<p>
<%= link_to 'Отредактируйте этот тест', edit_test_path(@test) %> | <%= link_to  'Назад', tests_path %>
</p>

<%= render 'shared/questions', test: @test %>
